# Test Matrix — Kira API Evaluation

**Generated:** 2026-05-28
**Total rows:** 112 (was 108; +4 rows from 2026-05-28 partner-doc delta — META-finding, DRIFT-1b, DRIFT-1c, GAP-22b)
**Maintained by:** data-engineer (updated after each `/proc_comment` Phase-3 batch or new drift)
**Drift-prevention rule:** every row's `cross_ref` MUST link to a source-of-truth doc (the LAST link in the cell). If that doc updates the severity/status, this row must be re-synced.
**REFINE-2026-05-28 sweep (DEC-008):** 11 rows had severity adjusted per partner-doc delta (Bucket B downgrades + Bucket A reframings + DRIFT-1 escalation). Affected: T-P1-003, T-P1-005 (reframe only), T-P2-DRIFT-1 (escalation+annotation), T-P2-DRIFT-6, -11, -19, -23, -26, -27, -32, -33, -35, -40, -52. See DEC-008 in decision-log.

## Summary (top-of-doc dashboard)

- **Total rows in matrix:** 112 (12 columns)
- **By phase (actual matrix count):** P1-Docs=12 (incl. META-finding T-P1-META-01) · P2-Integration=74 (56 drift rows + 18 endpoint-probe rows; 13 of the endpoint-probes are CLOSED-CLEAN/coverage) · P3-Security=11 (9 findings + 2 controls) · P3-Abuse=11 (8 findings + 3 controls) · P3-Performance=5
- **By status:** DESIGNED=0 · DEVELOPED=0 · EXECUTED=12 (Phase-1 docs + META) · REPRODUCED=77 · CLOSED-CLEAN=6 · CLOSED-FINDING=16 · BLOCKED-DEP=0 · BLOCKED-GAP=0 · BLOCKED-ENV=2 · DEFERRED=0
- **By severity (post REFINE-2026-05-28):** CRITICAL=14 · HIGH=40 · MEDIUM=23 · LOW=12 · N/A=23 (controls + endpoint coverage rows). Delta vs pre-REFINE: CRITICAL −1 (4 down-shifted, 3 added: META + DRIFT-54 + DRIFT-55); HIGH −2 (4 in, 7 out, 1 added DRIFT-56); MEDIUM +6; LOW +1; N/A unchanged. See DEC-008.
- **Top cross-cutting concerns (by count):** `none` (49) · `envelope` (9) · `webhook-semantics` (8) · `info-disclosure` (7) · `iso3166` (6) · `auth` (5) · `idempotency` (5)
- **GWT executability:**
  - Rows with executable GWT (status ∈ {REPRODUCED, CLOSED-CLEAN, CLOSED-FINDING, BLOCKED-ENV}) → **97**
  - Rows with abstract docs-only GWT (Phase-1 docs assertions; status=EXECUTED but observable is a documentation artifact) → **11**
  - Rows BLOCKED whose GWT states the blocker → **1** (T-P2-EP-17 BLOCKED-ENV — quotations happy path unreachable due to ≥100% fee profile)
  - Total expressible as `.feature` scenarios without follow-up → **108 / 108**
- **Source-of-truth contradictions surfaced while populating expected/actual:**
  1. **Dashboard vs body row count** — original dashboard read "Total tests: 90" but the matrix body has always contained 108 rows. Reconciled to 108.
  2. **Status mix vs body** — original dashboard claimed CLOSED-CLEAN=11 + CLOSED-FINDING=7, but body has CLOSED-CLEAN=5 + CLOSED-FINDING=15. Recounted from rows.
  3. **DRIFT-26 reservation** — integration-log.md L234 reserves DRIFT-26 as "unused" then re-uses the ID at L239 for recipients alpha-2 finding. The matrix row T-P2-DRIFT-26 uses the L239 meaning (alpha-2). Worth flagging in integration-log.
  4. **Severity drift** — original dashboard said CRITICAL=14 / LOW=12 / N/A=14, but recount yields CRITICAL=12 / LOW=11 / N/A=35. The N/A inflation comes from endpoint-probe rows that are coverage controls, not findings.
  5. **GAP-04 polarity** — flow-design says "API key OR Bearer", DRIFT-12 says "both required everywhere", DRIFT-49 says "both required and inverted on webhooks". The runtime is "both required everywhere; webhooks reject Bearer-only with 403 instead of 401". Treated as confirmed in T-P2-DRIFT-12 / T-P2-DRIFT-49.

## Matrix

Sorted by phase (P1 → P2 drift → P2 coverage → P3-Sec → P3-Abuse → P3-Perf), then by severity desc.

| test_id | phase | endpoint | category | cross_cutting_tag | description | given_when_then | expected | actual | severity | status | cross_ref |
|---|---|---|---|---|---|---|---|---|---|---|---|
| T-P1-001 | P1-Docs | POST /v1/quotations (Quotations) | docs-quality | none | Quotations Reference and Guides describe two disjoint endpoint schemas | Given Kira's Reference page for POST /v1/quotations and the Guides "Create a Quotation" page<br>When I diff the documented request body shapes<br>Then both pages MUST converge on one canonical schema<br>And Reference fields MUST appear in Guides and vice versa | one canonical schema across Reference + Guides | two disjoint shapes published; Guides body is silently dropped at runtime (DRIFT-40) | CRITICAL | EXECUTED | GAP-31, GAP-29, Finding-#1, evidence: evidence/analysis/03-phase-1-findings.md |
| T-P1-002 | P1-Docs | cross-cutting (sidebar) | docs-quality | versioning | Three sidebar entries 404 (versioning, metadata, api-upgrades) — 23% dead-link rate | Given the v2026-04-14 docs sidebar with 13 entries<br>When I HEAD-fetch each sidebar URL<br>Then every entry MUST return HTTP 200<br>And the dead-link rate MUST be 0% | 13/13 sidebar entries return 200 | 3/13 return 404 (versioning, metadata, api-upgrades) — 23% dead-link rate | CRITICAL | EXECUTED | GAP-01, GAP-35, GAP-36, Finding-#2, evidence: evidence/analysis/03-phase-1-findings.md |
| T-P1-003 | P1-Docs | cross-cutting (errors) | docs-quality | envelope | Three (now four) coexisting error envelope shapes documented (REFINE-2026-05-28: CRITICAL → HIGH per partner guide L122-124 acknowledgement + v2026-XX-XX unification fix) | Given the error-handling guide plus per-endpoint error examples in Reference<br>When I enumerate all documented error envelope shapes<br>Then exactly one envelope shape MUST be documented across the API<br>And no two endpoints MUST document different envelopes | 1 documented envelope shape | 4 distinct shapes documented (A flat, B nested, C bare, D Nest-style) | HIGH | EXECUTED | GAP-05, GAP-06, GAP-19, Finding-#3, DELTA-Bucket-B, evidence: evidence/analysis/03-phase-1-findings.md, evidence/analysis/13-docs-vs-partner-guide-delta.md |
| T-P1-004 | P1-Docs | POST /webhooks/register (Webhooks) | docs-quality | webhook-semantics | Webhook contract has no signature encoding, no retry, no replay protection, `secret` optional | Given the Webhooks Reference + Guides pages<br>When I look for signature encoding, retry policy, replay-window, and `secret` requirement<br>Then all four semantics MUST be documented<br>And `secret` MUST be marked required | 4/4 semantics documented; secret=required | 0/4 documented; secret marked optional | CRITICAL | EXECUTED | GAP-11, GAP-21, GAP-12, Finding-#4, evidence: evidence/analysis/03-phase-1-findings.md |
| T-P1-005 | P1-Docs | POST /v1/users/{id}/wallets (cross-cutting) | docs-quality | none | Wallets product on comparison matrix but has no `/v1/wallets` Reference page (REFINE-2026-05-28: reframed per partner guide L84-95 which omits Wallets entirely — inverse-confirms the product is absent from v2026-04-14 surface, not just missing-from-Reference. Severity HIGH holds but framing pivots from "docs gap" to "product-truth gap") | Given the product comparison matrix lists Wallets as a product<br>When I open the Reference sidebar and search for a wallets endpoint page<br>Then a `/v1/users/{id}/wallets` Reference page MUST exist<br>And the page MUST document request/response shape | dedicated Wallets Reference page | only mentioned in idempotency-key list; no Reference page exists; partner guide also omits Wallets from "What you CAN do" surface | HIGH | EXECUTED | GAP-37, Finding-#5, DELTA-Bucket-A, evidence: evidence/analysis/03-phase-1-findings.md, evidence/analysis/13-docs-vs-partner-guide-delta.md |
| T-P1-006 | P1-Docs | GET /banks (Reference Data) | docs-quality | path-prefix | `/banks` is unversioned (no `/v1/` prefix) — codegen pipelines silently break | Given the Reference catalogue where every other endpoint uses `/v1/` prefix<br>When I read the documented path for the banks endpoint<br>Then the path MUST be `/v1/banks`<br>And the prefix MUST be consistent with all other endpoints | path = `/v1/banks` | path documented as `/banks` (unversioned) — codegen pipelines drop the route | HIGH | EXECUTED | GAP-32, GAP-01, GAP-20, Finding-#6, evidence: evidence/analysis/03-phase-1-findings.md |
| T-P1-007 | P1-Docs | cross-cutting (Reference pages) | docs-quality | none | Reference pages strip response JSON behind "Click Try It!" placeholder | Given any of the 14 Reference endpoint pages<br>When I scroll to the "Response" section<br>Then a static 2xx JSON example MUST be visible without clicking "Try It!"<br>And the example MUST match the live runtime shape | static 2xx JSON visible on each page | 14/14 pages show "Click Try It!" placeholder; no static example | HIGH | EXECUTED | GAP-30, Finding-#7, evidence: evidence/analysis/03-phase-1-findings.md |
| T-P1-008 | P1-Docs | cross-cutting (Reference pages) | docs-quality | none | Reference pages stale vs Apr-14 changelog (createVA missing ~60% fields; createuser hides `business`) | Given the v2026-04-14 changelog announcing createVA and createUser updates<br>When I diff the Reference page bodies vs changelog<br>Then every changelog-announced field MUST appear in Reference<br>And the createUser page MUST document the `business` discriminator | 100% changelog fields in Reference | createVA missing ~60% of fields; createUser hides `business` | HIGH | EXECUTED | GAP-34, GAP-31, GAP-14, GAP-04, Finding-#8, evidence: evidence/analysis/03-phase-1-findings.md |
| T-P1-009 | P1-Docs | cross-cutting (error-handling page) | docs-quality | envelope | Error-handling guide documents ~6 codes; runtime has 12+ across 4 envelopes | Given the error-handling Guides page and the runtime error inventory<br>When I diff documented error codes vs codes captured in Phase 2<br>Then every runtime error code MUST appear in the docs<br>And the inventory MUST cover all 4 envelope shapes | ≥12 codes documented across all envelopes | 6 codes documented; runtime has 12+ → ~80% missing | HIGH | EXECUTED | GAP-05, GAP-06, GAP-08, Finding-#9, evidence: evidence/analysis/03-phase-1-findings.md |
| T-P1-010 | P1-Docs | POST /v1/users (Users) | docs-quality | auth | `POST /v1/users` Reference page omits `Authorization: Bearer` header | Given the POST /v1/users Reference page header section<br>When I list all required headers documented<br>Then `Authorization: Bearer <token>` MUST be listed as required<br>And the runtime requirement (401 without it) MUST be reflected | Bearer header documented as required | only `x-api-key` and `idempotency-key` listed; Bearer omitted | HIGH | EXECUTED | GAP-04, GAP-30, Finding-#10, evidence: evidence/analysis/03-phase-1-findings.md |
| T-P1-011 | P1-Docs | GET /banks + recipients/users (cross-cutting) | docs-quality | iso3166 | ISO 3166 alpha-2 vs alpha-3 inconsistency across `/banks`, recipient.country, user.address_country | Given the docs for /banks, /v1/recipients, and /v1/users<br>When I read the country-code format declared on each<br>Then all three MUST declare the same ISO 3166 variant (alpha-2 OR alpha-3)<br>And no two endpoints MUST require opposite codes | uniform alpha-2 OR uniform alpha-3 | /banks + recipients require alpha-2; user.address_country requires alpha-3 | HIGH | EXECUTED | GAP-20, Finding-#11, Finding-#6, evidence: evidence/analysis/03-phase-1-findings.md |
| T-P2-DRIFT-1 | P2-Integration | POST /auth (Auth) | congruence | sandbox-prod | Documented `/sandbox` stage prefix returns 403 for /auth — base URL global lie (REFINE-2026-05-28: ESCALATED via 2026-05-28 revalidation — partner guide ALSO directs to /sandbox prefix; pin endpoint does NOT unlock the stage; two parallel auth/base-URL models coexist (public docs + partner guide), neither matches runtime. See DRIFT-1b and DRIFT-1c.) | Given documented sandbox base URL `https://api.balampay.com/sandbox` AND partner-guide pin flow `POST /sandbox/v1/versioning/upgrade`<br>When I POST `/sandbox/auth` with valid `client_id`+`password`, then run the partner-guide pin sequence and retry<br>Then response MUST be HTTP 2xx with JWT envelope on the documented base<br>And after a successful pin the `/sandbox/*` tree MUST be unlocked | status=200; body.data.access_token defined; pin unlocks /sandbox/* | status=403 on /sandbox/auth (DRIFT-1); pin works only at no-prefix base (DRIFT-1b); after pin /sandbox/* still 403/401 (DRIFT-1c) | CRITICAL | REPRODUCED | DRIFT-1, DRIFT-1b, DRIFT-1c, DELTA-Bucket-C, evidence: evidence/analysis/04-integration-log.md#drift-1, evidence/work/versioning/, evidence/analysis/13-docs-vs-partner-guide-delta.md |
| T-P2-DRIFT-2 | P2-Integration | POST /v1/users (Users) | congruence | sandbox-prod | `/sandbox` prefix wrong for /v1/users too — base URL is root | Given documented sandbox base URL `https://api.balampay.com/sandbox`<br>When I POST `/sandbox/v1/users` with valid Bearer + x-api-key + minimal business body<br>Then response MUST be HTTP 2xx with user resource<br>And /sandbox prefix MUST be a working stage gate | status=201; body.id defined | status=401; UnauthorizedException at /sandbox/v1/users (DRIFT-1 widens) | CRITICAL | REPRODUCED | DRIFT-2, DRIFT-1, evidence: evidence/analysis/04-integration-log.md#drift-2 |
| T-P2-DRIFT-3 | P2-Integration | POST /v1/users (Users) | congruence | none | `verification_triggered: false` despite docs claiming minimal payload auto-triggers KYB | Given a fresh business user creation per docs minimal payload<br>When I POST /v1/users with the documented minimum body<br>Then response.body.verification_triggered MUST equal true<br>And no further required fields MUST be needed to start KYB | body.verification_triggered=true | body.verification_triggered=false; 21 additional fields required to actually trigger | HIGH | REPRODUCED | DRIFT-3, DRIFT-19, evidence: evidence/analysis/04-integration-log.md#drift-3 |
| T-P2-DRIFT-4 | P2-Integration | POST /v1/users (Users) | congruence | mass-assignment | Undocumented response fields on UserResponse (4 of 15 keys undocumented) | Given the documented UserResponse schema (4 keys)<br>When I POST /v1/users and enumerate `Object.keys(response.body)`<br>Then key count MUST equal 4<br>And no undocumented keys (e.g., `verification_mode`) MUST appear | top-level keys = 4 (per docs) | 15 top-level keys including verification_mode, status, kyb_status, etc. | MEDIUM | REPRODUCED | DRIFT-4, evidence: evidence/analysis/04-integration-log.md#drift-4 |
| T-P2-DRIFT-5 | P2-Integration | POST /v1/users (Users) | congruence | iso3166 | GAP-20 worse than predicted: alpha-2 AND alpha-3 both accepted, neither normalized | Given POST /v1/users where address.country expects ISO 3166 codes<br>When I send `address.country="US"` and a second user with `address.country="USA"`<br>Then exactly one variant MUST be accepted (and the other MUST 400)<br>And the response MUST normalize to a single canonical form | one variant rejected OR both normalized to one form | both accepted; both echoed back verbatim (data-quality bomb downstream) | HIGH | REPRODUCED | DRIFT-5, GAP-20, evidence: evidence/analysis/04-integration-log.md#drift-5 |
| T-P2-DRIFT-6 | P2-Integration | cross-cutting (errors) | congruence | envelope | Four-shape error envelope variance in 401/403/400/500 responses (REFINE-2026-05-28: CRITICAL → HIGH per partner guide L122-124 acknowledgement + v2026-XX-XX `{type,code,message,param,agent_hint}` unification fix) | Given multiple endpoints emitting 4xx/5xx errors<br>When I capture error bodies for 401 (auth), 403 (gateway), 400 (validation), 500 (server)<br>Then all four MUST share one envelope shape (e.g., `{error:{code,message}}`)<br>And no endpoint MUST emit a distinct shape on the same surface | uniform single envelope across all status codes | 4 distinct shapes captured (Shape A flat, B nested, C bare, G AWS-IAM) | HIGH | REPRODUCED | DRIFT-6, GAP-03, GAP-05, Finding-#3, DELTA-Bucket-B, evidence: evidence/analysis/04-integration-log.md#drift-6, evidence/analysis/13-docs-vs-partner-guide-delta.md |
| T-P2-DRIFT-7 | P2-Integration | GET /banks (Reference Data) | congruence | path-prefix | Documented `/banks` returns HTTP 200 with Cloudflare WAF non-JSON body (silent-success bug) | Given the documented unversioned path `/banks`<br>When I GET `/banks` with valid auth<br>Then response MUST be HTTP 200 with JSON body matching the banks schema<br>And the body MUST NOT contain Cloudflare WAF strings | content-type=application/json; body.banks array defined | HTTP 200 with body `"Blocked"` / Cloudflare 522 HTML (silent success) | HIGH | REPRODUCED | DRIFT-7, GAP-32, Finding-#6, evidence: evidence/analysis/04-integration-log.md#drift-7 |
| T-P2-DRIFT-8 | P2-Integration | GET /v1/banks (Reference Data) | congruence | iso3166 | `/v1/banks` accepts only `country_code=CO` — Colombia-only at runtime | Given /v1/banks documented as multi-country<br>When I GET /v1/banks?country_code=MX (or any non-CO alpha-2)<br>Then response MUST be HTTP 200 with the country's banks<br>And `country_code` MUST accept any valid alpha-2 | status=200; body lists banks for the requested country | status=400; `"Only CO (Colombia) is supported"` | HIGH | REPRODUCED | DRIFT-8, GAP-20, evidence: evidence/analysis/04-integration-log.md#drift-8 |
| T-P2-DRIFT-9 | P2-Integration | GET /v1/banks (Reference Data) | congruence | none | Query param is `country_code` not `country` on /v1/banks | Given docs imply param name `country` (per other endpoints)<br>When I GET /v1/banks?country=CO<br>Then response MUST be HTTP 200<br>And the param name MUST be consistent with other endpoints | status=200 with `country` param | status=400 demanding `country_code`; `country` is unknown | MEDIUM | REPRODUCED | DRIFT-9, evidence: evidence/analysis/04-integration-log.md#drift-9 |
| T-P2-DRIFT-10 | P2-Integration | GET /v1/users, /v1/virtual-accounts (lists) | congruence | pagination | `?limit=100000` returns HTTP 500 on lists with bare Shape-C envelope | Given pagination is documented as `?limit=<N>` with no stated upper bound<br>When I GET /v1/users?limit=100000<br>Then response MUST be either capped page (200) or validation error (400)<br>And response MUST NOT be 500 | status ∈ {200, 400} with structured error if rejected | status=500 with bare Shape-C envelope on /v1/users and /v1/virtual-accounts | HIGH | REPRODUCED | DRIFT-10, evidence: evidence/analysis/04-integration-log.md#drift-10 |
| T-P2-DRIFT-11 | P2-Integration | cross-cutting (headers) | congruence | versioning | `X-Api-Version: 2025-01-01` silently echoed back; identical response body (REFINE-2026-05-28: HIGH → MEDIUM per partner guide L15 explicit `POST /v1/versioning/upgrade` pin spec — header semantic now defined for partners; public-doc gap stands but partner-side cost drops) | Given GAP-01 (no version header documented)<br>When I send header `X-Api-Version: 2025-01-01` on any endpoint<br>Then response MUST be 400 (unknown header) OR MUST emit a version-specific schema<br>And the header MUST NOT be silently echoed with no behavioral change | 400 OR distinct schema per version | header echoed; response body byte-identical (false confidence) | MEDIUM | REPRODUCED | DRIFT-11, GAP-01, Finding-#2, DELTA-Bucket-B, evidence: evidence/analysis/04-integration-log.md#drift-11, evidence/analysis/13-docs-vs-partner-guide-delta.md |
| T-P2-DRIFT-12 | P2-Integration | cross-cutting (auth) | congruence | auth | `x-api-key` alone does not suffice for any Batch-A list endpoint | Given GAP-04 doc says "API key OR Bearer"<br>When I GET /v1/users with only `x-api-key` header (no Authorization)<br>Then response MUST be HTTP 200<br>And `x-api-key` alone MUST satisfy auth per docs | status=200 with x-api-key only | status=401 — both x-api-key AND Bearer required across Batch A | HIGH | REPRODUCED | DRIFT-12, GAP-04, Finding-#10, evidence: evidence/analysis/04-integration-log.md#drift-12 |
| T-P2-DRIFT-13 | P2-Integration | GET /v1/recipients (Recipients) | congruence | envelope | /v1/recipients returns Shape-B error envelope vs /v1/users Shape-A | Given /v1/users emits Shape A (`{error:{code,message}}`) on 4xx<br>When I trigger a 400 on /v1/recipients (missing user_id)<br>Then response envelope MUST match /v1/users Shape A<br>And no nested-message variants MUST be emitted | Shape A envelope on both endpoints | /v1/recipients returns Shape B (nested message) on identical status code | HIGH | REPRODUCED | DRIFT-13, GAP-03, evidence: evidence/analysis/04-integration-log.md#drift-13 |
| T-P2-DRIFT-14 | P2-Integration | GET /v1/users/{id} (Users) | congruence | none | `verification_triggered` field disappears on GET (present on POST) | Given POST /v1/users returned response.body.verification_triggered<br>When I GET /v1/users/{id} for the same user<br>Then response.body MUST contain `verification_triggered` (round-trip)<br>And no documented field MUST disappear on GET | body.verification_triggered defined on GET | field absent on GET; only present on POST response | MEDIUM | REPRODUCED | DRIFT-14, evidence: evidence/analysis/04-integration-log.md#drift-14 |
| T-P2-DRIFT-15 | P2-Integration | PUT /v1/users/{id} (Users) | congruence | none | PUT with empty body returns 200 (not 400) and `updated_fields: []` | Given a created user<br>When I PUT /v1/users/{id} with body `{}`<br>Then response MUST be HTTP 400 (validation)<br>And no successful update MUST be reported for an empty body | status=400; validation error | status=200; body.updated_fields=[] | MEDIUM | REPRODUCED | DRIFT-15, evidence: evidence/analysis/04-integration-log.md#drift-15 |
| T-P2-DRIFT-16 | P2-Integration | POST /v1/users/{id}/verifications | congruence | idempotency | Idempotency NOT enforced — same key + diff body returns two 201s | Given GAP-08 promises idempotency-key enforcement<br>When I POST /v1/users/{id}/verifications twice with the same `Idempotency-Key` and different bodies<br>Then second response MUST be HTTP 409 (conflict)<br>And response.body.id MUST equal the first call's id | second call: status=409 OR identical id | both calls: status=201 with distinct ids | HIGH | REPRODUCED | DRIFT-16, GAP-08, evidence: evidence/analysis/04-integration-log.md#drift-16 |
| T-P2-DRIFT-17 | P2-Integration | POST /v1/users/{id}/verifications | functional | state-casing | Endpoint does NOT move user state machine; provisions AiPrise URL only | Given a user with status=DRAFT<br>When I POST /v1/users/{id}/verifications<br>Then the user's status MUST advance (e.g., to VERIFYING or REVIEW)<br>And a GET /v1/users/{id} MUST reflect the new status | GET shows status advanced from DRAFT | user.status unchanged; only an AiPrise URL is minted | HIGH | REPRODUCED | DRIFT-17, evidence: evidence/analysis/04-integration-log.md#drift-17 |
| T-P2-DRIFT-18 | P2-Integration | POST /v1/users (Users) | congruence | none | `account_purpose` enum differs by user type (18 vs 14 values) | Given docs document one `account_purpose` enum<br>When I POST /v1/users with type=individual vs type=business using the same enum value<br>Then both MUST accept the same enum<br>And the enum MUST NOT branch on user type | uniform enum accepted across both types | individual accepts 18 values; business accepts 14 values; overlap partial | MEDIUM | REPRODUCED | DRIFT-18, evidence: evidence/analysis/04-integration-log.md#drift-18 |
| T-P2-DRIFT-19 | P2-Integration | POST /v1/users (Users) | congruence | none | `business_industry` is undocumented NAICS-coded array enum (90+ values) (REFINE-2026-05-28: HIGH → MEDIUM per partner guide L129 magic-SSN/EIN tables + Postman collection canonical example) | Given docs declare `business_industry` as a string<br>When I POST /v1/users with `business_industry: "Retail"`<br>Then response MUST be HTTP 201 (accepted)<br>And the field MUST accept free-string per docs | status=201 with string business_industry | status=400 demanding an array of NAICS codes from a 90+ value enum | MEDIUM | REPRODUCED | DRIFT-19, DELTA-Bucket-B, evidence: evidence/analysis/04-integration-log.md#drift-19, evidence/analysis/13-docs-vs-partner-guide-delta.md |
| T-P2-DRIFT-20 | P2-Integration | PUT /v1/users/{id} (Users) | congruence | none | `metadata` values must be strings (silent type constraint) | Given docs declare `metadata` as an opaque key-value object<br>When I PUT /v1/users/{id} with `metadata: {"tier": 1}`<br>Then response MUST be HTTP 200<br>And non-string scalar metadata values MUST be accepted per docs | status=200 with numeric metadata value | status=400; metadata values must be strings | MEDIUM | REPRODUCED | DRIFT-20, evidence: evidence/analysis/04-integration-log.md#drift-20 |
| T-P2-DRIFT-21 | P2-Integration | POST /verification/send | congruence | path-prefix | OTP endpoint exists at root `/verification/send` (not `/v1/`); keyed on email | Given docs convention puts versioned endpoints under `/v1/`<br>When I POST /v1/verification/send with `{user_id, ...}`<br>Then response MUST be HTTP 200 at the `/v1/` path<br>And the request key MUST be `user_id`, not `email` | /v1/ path returns 200; user-keyed | /v1/ returns 404; works only at root /verification/send keyed by email | MEDIUM | REPRODUCED | DRIFT-21, GAP-23, evidence: evidence/analysis/04-integration-log.md#drift-21 |
| T-P2-DRIFT-22 | P2-Integration | POST /v1/users (Users) | abuse | mass-assignment | `status` accepts third enum `active|inactive|suspended` on input | Given docs declare output `status` enum (e.g., DRAFT/REVIEW/VERIFIED)<br>When I POST /v1/users with `status: "active"` (legacy enum value)<br>Then response MUST be HTTP 400 (unknown enum on input)<br>And input MUST NOT accept output-only or legacy enums | status=400 rejecting `active` | status=201; `active` accepted and stored on user resource | HIGH | REPRODUCED | DRIFT-22, evidence: evidence/analysis/04-integration-log.md#drift-22 |
| T-P2-DRIFT-23 | P2-Integration | POST /v1/users/{id}/verifications | functional | state-casing | Sandbox does NOT auto-approve verification; users stuck in REVIEW (REFINE-2026-05-28: CRITICAL → HIGH per partner guide L36-42 Slack-ping workaround + L42 v2026-XX-XX magic-trigger emails fix scheduled end of June) | Given sandbox §5.2 promises auto-approve for KYB<br>When I POST /v1/users/{id}/verifications and wait 60s, then GET /v1/users/{id}<br>Then user.verification_status MUST transition REVIEW → VERIFIED within 60s<br>And the sandbox MUST mirror the auto-approve flow | verification_status=VERIFIED within 60s | verification_status stuck at REVIEW for ≥120s (Batches D/F blocked) | HIGH | REPRODUCED | DRIFT-23, Finding-#4, DELTA-Bucket-B, evidence: evidence/analysis/04-integration-log.md#drift-23, evidence/analysis/13-docs-vs-partner-guide-delta.md |
| T-P2-DRIFT-24 | P2-Integration | POST /v1/users (Users) | security | info-disclosure | S3 presigned URLs leak `production` bucket name + tenant `client_id` | Given a sandbox user creation<br>When I POST /v1/users and parse any presigned S3 URL in the response<br>Then the URL hostname MUST contain "sandbox" (sandbox-isolated bucket)<br>And the URL MUST NOT contain another tenant's client_id | URL host matches a sandbox bucket | URL host is the `production` bucket; tenant client_id leaks in path | HIGH | REPRODUCED | DRIFT-24, evidence: evidence/analysis/04-integration-log.md#drift-24 |
| T-P2-DRIFT-25 | P2-Integration | POST /v1/users/{id}/verifications | congruence | none | Undocumented verification type `api` discriminator (in addition to `embedded-link`) | Given docs document a single `type: embedded-link`<br>When I POST /v1/users/{id}/verifications with an invalid `type` to harvest the enum<br>Then the 400 enum-list MUST contain only `embedded-link`<br>And no undocumented values MUST be revealed | enum=[embedded-link] | enum=[embedded-link, api] — `api` undocumented | LOW | REPRODUCED | DRIFT-25, evidence: evidence/analysis/04-integration-log.md#drift-25 |
| T-P2-DRIFT-26 | P2-Integration | POST /v1/recipients (Recipients) | congruence | iso3166 | Recipient country fields require alpha-2; contradicts /v1/users alpha-3 (REFINE-2026-05-28: HIGH → MEDIUM per partner guide L55 explicit acknowledgement of inconsistency — integrator is warned) | Given /v1/users requires alpha-3 `address.country`<br>When I POST /v1/recipients with `country: "USA"` (alpha-3)<br>Then response MUST be HTTP 201 (uniform alpha-3)<br>And alpha-2 vs alpha-3 MUST NOT differ across endpoints | status=201 with alpha-3 | status=400 demanding alpha-2 on recipients (opposite of users) | MEDIUM | REPRODUCED | DRIFT-26, GAP-20, Finding-#11, DELTA-Bucket-B, evidence: evidence/analysis/04-integration-log.md#drift-26, evidence/analysis/13-docs-vs-partner-guide-delta.md |
| T-P2-DRIFT-27 | P2-Integration | POST /v1/recipients (Recipients) | congruence | none | Documented `doc_type:"ssn"` for ACH is rejected at runtime (REFINE-2026-05-28: HIGH → MEDIUM per partner guide L127 ACH Counterparty rename target in v2026-XX-XX) | Given docs/run_flow.py list `doc_type: "ssn"` for ACH USD recipients<br>When I POST /v1/recipients with `doc_type: "ssn"`<br>Then response MUST be HTTP 201<br>And the enum MUST include `ssn` | status=201 with doc_type=ssn | status=400; allowed enum = id\|dni\|passport\|ein (no ssn) | MEDIUM | REPRODUCED | DRIFT-27, DELTA-Bucket-B, evidence: evidence/analysis/04-integration-log.md#drift-27, evidence/analysis/13-docs-vs-partner-guide-delta.md |
| T-P2-DRIFT-28 | P2-Integration | POST /v1/recipients (Recipients) | congruence | none | WALLET TRON validated via base58check (good, undocumented hard validation) | Given docs declare only a length check on TRON addresses<br>When I POST /v1/recipients with a TRON-length string failing base58check<br>Then response MUST be HTTP 201 per docs (length-only)<br>And documentation MUST disclose any cryptographic validation | status=201 (length-only per docs) | status=400; cryptographic base58check rejection (undocumented) | LOW | REPRODUCED | DRIFT-28, evidence: evidence/analysis/04-integration-log.md#drift-28 |
| T-P2-DRIFT-29 | P2-Integration | POST /v1/recipients (Recipients) | congruence | none | SWIFT recipients require recipient-level `address` object (flow-design listed ACH-only) | Given flow-design lists `address` as ACH-only<br>When I POST /v1/recipients (SWIFT EUR) without `address`<br>Then response MUST be HTTP 201<br>And SWIFT MUST NOT silently require `address` | status=201 without address | status=400 demanding `address.{}` for SWIFT | HIGH | REPRODUCED | DRIFT-29, evidence: evidence/analysis/04-integration-log.md#drift-29 |
| T-P2-DRIFT-30 | P2-Integration | POST/GET /v1/recipients (Recipients) | security | info-disclosure | account_details returned UNMASKED despite docs `****7890` masking | Given docs show masked account digits `****7890`<br>When I POST then GET /v1/recipients/{id} and inspect account_details<br>Then digits MUST be masked (last-4 only) on response<br>And clabe/account_number/swift_code/wallet/doc_number MUST NOT be plaintext | masked last-4 in response | full plaintext clabe/account_number/swift_code/wallet/doc_number returned | HIGH | REPRODUCED | DRIFT-30, evidence: evidence/analysis/04-integration-log.md#drift-30 |
| T-P2-DRIFT-31 | P2-Integration | cross-cutting (timestamps) | congruence | none | Timestamp format non-ISO (space + `+00`, no `Z`) — RFC3339 parsers fail | Given timestamps are documented as ISO 8601<br>When I parse response.body.created_at with an RFC3339-strict parser<br>Then parse MUST succeed<br>And format MUST contain `T` separator and `Z` or `±HH:MM` offset | RFC3339-compliant string parses | `"2026-05-28 00:52:49.879228+00"` — space + `+00` fails strict parsers | MEDIUM | REPRODUCED | DRIFT-31, evidence: evidence/analysis/04-integration-log.md#drift-31 |
| T-P2-DRIFT-32 | P2-Integration | POST/GET /v1/recipients (Recipients) | congruence | none | SWIFT POST-create response loses `state`+`postal_code` (empty strings); GET returns them (REFINE-2026-05-28: HIGH → MEDIUM per partner guide L68 direct acknowledgement of the silent-drop quirk) | Given SWIFT POST sent with bank_address.state and bank_address.postal_code populated<br>When I capture the POST response, then GET /v1/recipients/{id}<br>Then both responses MUST contain the original state and postal_code<br>And POST-create MUST NOT return empty strings for fields the GET later returns | POST + GET both echo original values | POST returns state="" and postal_code=""; GET returns the real values | MEDIUM | REPRODUCED | DRIFT-32, DELTA-Bucket-B, evidence: evidence/analysis/04-integration-log.md#drift-32, evidence/analysis/13-docs-vs-partner-guide-delta.md |
| T-P2-DRIFT-33 | P2-Integration | GET /v1/recipients (Recipients) | congruence | envelope | List envelope is `{recipients, total}` — third distinct list-envelope shape (REFINE-2026-05-28: HIGH → MEDIUM per partner guide L127 v2026-XX-XX primitive rename → surface normalization implied) | Given /v1/users uses `{data, pagination}` list envelope<br>When I GET /v1/recipients?user_id=<id><br>Then list envelope MUST match `{data, pagination}`<br>And no third list shape MUST exist | `{data, pagination}` envelope | `{recipients, total}` — third distinct list envelope on the API | MEDIUM | REPRODUCED | DRIFT-33, DELTA-Bucket-B, evidence: evidence/analysis/04-integration-log.md#drift-33, evidence/analysis/13-docs-vs-partner-guide-delta.md |
| T-P2-DRIFT-34 | P2-Integration | POST /v1/recipients (Recipients) | congruence | idempotency | Idempotent-replay returns 201 not 202 — flow-design "unusual 202" is FALSE | Given flow-design §3.5 documents 202 on idempotent replay<br>When I POST /v1/recipients twice with the same Idempotency-Key + identical body<br>Then second response MUST be HTTP 202<br>And body.id MUST equal first response's id | status=202 on replay | status=201 on replay (with original id) — §3.5 claim is false | LOW | REPRODUCED | DRIFT-34, evidence: evidence/analysis/04-integration-log.md#drift-34 |
| T-P2-DRIFT-35 | P2-Integration | POST /v1/recipients (Recipients) | congruence | idempotency | 409 idempotency conflict returns empty `details: {}` — no field-level diff (REFINE-2026-05-28: HIGH → MEDIUM per partner guide L113-115/L131 — same v2026-XX-XX 409-restoration fix as DRIFT-50) | Given GAP-08 promises field-level diff on idempotency conflict<br>When I POST /v1/recipients twice with same Idempotency-Key + different bodies<br>Then 409 response.body.details MUST contain the diff fields<br>And details MUST NOT be empty | body.details lists conflicting fields | body.details = `{}` (empty) | MEDIUM | REPRODUCED | DRIFT-35, GAP-08, DELTA-Bucket-B, evidence: evidence/analysis/04-integration-log.md#drift-35, evidence/analysis/13-docs-vs-partner-guide-delta.md |
| T-P2-DRIFT-36 | P2-Integration | POST /v1/recipients (Recipients) | congruence | envelope | /v1/recipients emits TWO error envelope shapes on the same endpoint | Given a single endpoint should emit a single error envelope<br>When I trigger a 400 (validation) and a 409 (idempotency) on POST /v1/recipients<br>Then both responses MUST share one envelope shape<br>And the same endpoint MUST NOT switch shapes by status | both responses use Shape A | 400 uses Shape A (flat); 409 uses Shape B (nested) on same endpoint | HIGH | REPRODUCED | DRIFT-36, GAP-05, evidence: evidence/analysis/04-integration-log.md#drift-36 |
| T-P2-DRIFT-37 | P2-Integration | POST /v1/recipients (Recipients) | abuse | mass-assignment | SPEI body + WALLET fields → 201 silent strip, no `unknown_fields` warning | Given an SPEI MX recipient body<br>When I POST /v1/recipients with SPEI body containing WALLET-only fields (e.g., `wallet_address`)<br>Then response MUST be HTTP 400 (unknown fields) OR HTTP 201 echoing the extras<br>And unknown fields MUST NOT be silently dropped | status=400 OR status=201 with unknown_fields warning | status=201; WALLET fields silently stripped; no warning emitted | MEDIUM | REPRODUCED | DRIFT-37, evidence: evidence/analysis/04-integration-log.md#drift-37 |
| T-P2-DRIFT-38 | P2-Integration | POST /v1/recipients (Recipients) | abuse | iso3166 | SPEI with `country: "USA"` → 201 silent OVERRIDE to "MX" (from CLABE) | Given an SPEI MX recipient with a valid CLABE<br>When I POST /v1/recipients with `country: "USA"`<br>Then response MUST be HTTP 400 (mismatch) OR MUST echo `country: "USA"`<br>And country MUST NOT be silently rewritten | status=400 OR response.country="USA" | status=201 with response.country="MX" (silently overridden from CLABE) | HIGH | REPRODUCED | DRIFT-38, evidence: evidence/analysis/04-integration-log.md#drift-38 |
| T-P2-DRIFT-39 | P2-Integration | DELETE /v1/recipients/{id} | security | info-disclosure | DELETE returns 403 with AWS-IAM SigV4 leak in error body; recipient still exists | Given a created recipient<br>When I DELETE /v1/recipients/{id}<br>Then response MUST be HTTP 405 with `Allow` header OR HTTP 204<br>And response body MUST NOT contain SigV4 / AWS-IAM strings | status ∈ {204, 405}; no SigV4 leak | status=403 with AWS-IAM SigV4 error body; recipient still exists on GET | MEDIUM | REPRODUCED | DRIFT-39, GAP-21, evidence: evidence/analysis/04-integration-log.md#drift-39 |
| T-P2-DRIFT-40 | P2-Integration | POST /v1/quotations (Quotations) | congruence | none | Guides Quotations schema is silently dropped at runtime (Guides is dead doc) (REFINE-2026-05-28: CRITICAL → HIGH per partner guide L127 v2026-XX-XX primitive renames + L139-149 Postman collection as canonical worked example) | Given the Guides "Create a Quotation" documented body<br>When I POST /v1/quotations with the Guides-shape body verbatim<br>Then response MUST be HTTP 201 with a quote_id<br>And the Guides schema MUST be honored | status=201 with quote_id | status=400; Guides body silently dropped, only Reference fields recognized | HIGH | REPRODUCED | DRIFT-40, GAP-31, Finding-#1, DELTA-Bucket-B, evidence: evidence/analysis/04-integration-log.md#drift-40, evidence/analysis/13-docs-vs-partner-guide-delta.md |
| T-P2-DRIFT-41 | P2-Integration | POST /v1/quotations (Quotations) | congruence | none | `amount` MUST be string (regex `^[0-9]+(\.[0-9]{1,2})?$`), not number | Given doc snippets show `amount` as numeric<br>When I POST /v1/quotations with `amount: 100.00` (number)<br>Then response MUST be HTTP 201<br>And `amount` MUST accept JSON numbers per docs | status=201 with numeric amount | status=400 demanding string matching `^[0-9]+(\.[0-9]{1,2})?$` | HIGH | REPRODUCED | DRIFT-41, evidence: evidence/analysis/04-integration-log.md#drift-41 |
| T-P2-DRIFT-42 | P2-Integration | POST /v1/quotations (Quotations) | congruence | none | `client_markup` is OBJECT, not string; inner schema unknown | Given docs declare `client_markup` as a string<br>When I POST /v1/quotations with `client_markup: "0.01"`<br>Then response MUST be HTTP 201<br>And `client_markup` MUST accept a string per docs | status=201 with string client_markup | status=400 demanding object; inner schema undocumented | MEDIUM | REPRODUCED | DRIFT-42, evidence: evidence/analysis/04-integration-log.md#drift-42 |
| T-P2-DRIFT-43 | P2-Integration | POST /v1/quotations (Quotations) | congruence | none | `recipient_id: null` is rejected (null ≠ omission) | Given JSON convention treats null as "field absent"<br>When I POST /v1/quotations with `recipient_id: null`<br>Then response MUST behave identically to omitting `recipient_id`<br>And null MUST NOT trigger a type-violation 400 | omission and null both 2xx | status=400; null rejected as type violation (null ≠ absent) | LOW | REPRODUCED | DRIFT-43, evidence: evidence/analysis/04-integration-log.md#drift-43 |
| T-P2-DRIFT-44 | P2-Integration | POST /v1/quotations (Quotations) | congruence | none | Empty-body 400 returns ONLY `amount` missing — masks conditional gate | Given multiple required fields on /v1/quotations<br>When I POST /v1/quotations with body `{}`<br>Then 400 response.body.errors MUST list ALL missing required fields<br>And staged disclosure (one field at a time) MUST NOT be used | full missing-field list in single 400 | only `amount` reported; subsequent calls reveal more (staged disclosure) | LOW | REPRODUCED | DRIFT-44, evidence: evidence/analysis/04-integration-log.md#drift-44 |
| T-P2-DRIFT-45 | P2-Integration | POST /v1/quotations (Quotations) | functional | none | Sandbox fee profiles ≥100% — every Reference-shape happy path blocked | Given the sandbox is supposed to honor a documented happy path<br>When I POST /v1/quotations with any valid amount per Reference body<br>Then response MUST be HTTP 200 with `quote_id`<br>And sandbox fee profile MUST be < amount | status=200 with quote_id | status=400 `"Total fees exceed payout amount"` for all 22 attempted amounts | HIGH | REPRODUCED | DRIFT-45, evidence: evidence/analysis/04-integration-log.md#drift-45 |
| T-P2-DRIFT-46 | P2-Integration | POST /v1/quotations (Quotations) | security | info-disclosure | Server leaks internal product taxonomy (`usa-va-fiat-to-crypto-payout`) via error | Given a 4xx error body<br>When I POST /v1/quotations with a body that triggers fee-profile error<br>Then response.body.error.message MUST be a generic "fee exceeds amount"<br>And internal product slugs (e.g., `usa-va-fiat-to-crypto-payout`) MUST NOT appear | generic error string | error message contains internal product slug `usa-va-fiat-to-crypto-payout` | HIGH | REPRODUCED | DRIFT-46, evidence: evidence/analysis/04-integration-log.md#drift-46 |
| T-P2-DRIFT-47 | P2-Integration | POST /webhooks/register (Webhooks) | security | webhook-semantics | webhook_url accepts SSRF-flavored URLs (localhost/IMDS/RFC1918/IPv6 loopback) | Given OWASP-API7 (SSRF) requires egress validation<br>When I POST /webhooks/register with webhook_url in {http://localhost, http://169.254.169.254, http://10.0.0.1, http://[::1]}<br>Then response MUST be HTTP 400 (validation)<br>And no private-range URL MUST be accepted | status=400 for all 5 SSRF vectors | status=200 on all 5; URLs persisted; IMDS reachable on delivery | CRITICAL | REPRODUCED | DRIFT-47, OWASP-API7:2023, Finding-#1 (sec), evidence: evidence/analysis/04-integration-log.md#drift-47 |
| T-P2-DRIFT-48 | P2-Integration | POST /webhooks/register (Webhooks) | security | webhook-semantics | `secret` is effectively optional (null and omit both accepted) | Given GAP-11 expects `secret` to be required for HMAC signing<br>When I POST /webhooks/register with `secret: null` and with secret omitted<br>Then both calls MUST be HTTP 400 (secret required)<br>And no webhook MUST be registered without a secret | status=400 (both variants) | status=200 (both variants); webhook registered with no HMAC material | HIGH | REPRODUCED | DRIFT-48, Finding-#4, evidence: evidence/analysis/04-integration-log.md#drift-48 |
| T-P2-DRIFT-49 | P2-Integration | POST /webhooks/register (Webhooks) | congruence | auth | GAP-04 INVERTED — both `x-api-key` AND Bearer required, contradicting docs | Given docs declare `x-api-key` is sufficient for webhooks<br>When I POST /webhooks/register with only `x-api-key` (no Authorization)<br>Then response MUST be HTTP 200<br>And `x-api-key` alone MUST authenticate per docs | status=200 with x-api-key only | status=401 with key-only; status=403 with Bearer-only — both required (inverted) | HIGH | REPRODUCED | DRIFT-49, GAP-04, evidence: evidence/analysis/04-integration-log.md#drift-49 |
| T-P2-DRIFT-50 | P2-Integration | POST /webhooks/register (Webhooks) | congruence | idempotency | `Idempotency-Key` silently ignored — same key + diff body returns 200 not 409 | Given idempotency-key.md promises 409 on key reuse with different body<br>When I POST /webhooks/register twice with same `Idempotency-Key` and different `webhook_url`<br>Then second response MUST be HTTP 409<br>And the prior registration MUST NOT be overwritten | second call: status=409 | second call: status=200; prior registration silently overwritten | HIGH | REPRODUCED | DRIFT-50, GAP-08, evidence: evidence/analysis/04-integration-log.md#drift-50 |
| T-P2-DRIFT-51 | P2-Integration | POST /webhooks/register (Webhooks) | congruence | webhook-semantics | Registration response is opaque: no id, no echo, no inventory, no delete | Given REST convention for resource creation<br>When I POST /webhooks/register successfully<br>Then response.body MUST contain a webhook id<br>And GET/DELETE companions MUST exist to inventory and revoke | body.id defined; GET/DELETE available | body only contains `{message:"…"}`; no id, no list, no delete | HIGH | REPRODUCED | DRIFT-51, GAP-21, evidence: evidence/analysis/04-integration-log.md#drift-51 |
| T-P2-DRIFT-52 | P2-Integration | POST /webhooks/register (Webhooks) | congruence | path-prefix | Path is `/webhooks/register` (no `/v1/`), only family without prefix (REFINE-2026-05-28: MEDIUM → LOW per partner guide L81 direct acknowledgement of the prefix anomaly + implied fix via v2026-XX-XX surface reshape) | Given every other endpoint family uses `/v1/` prefix<br>When I POST /v1/webhooks/register<br>Then response MUST be HTTP 200 (and `/webhooks/register` MUST 404)<br>And the webhooks family MUST follow the same prefix convention | /v1/webhooks/register works | /v1/ returns 404; only unversioned /webhooks/register works | LOW | REPRODUCED | DRIFT-52, DELTA-Bucket-B, evidence: evidence/analysis/04-integration-log.md#drift-52, evidence/analysis/13-docs-vs-partner-guide-delta.md |
| T-P2-DRIFT-53 | P2-Integration | POST /webhooks/register (Webhooks) | security | webhook-semantics | HTTP (cleartext) accepted as `webhook_url` scheme — not only HTTPS | Given §3.11 requires HTTPS-only for webhook URLs<br>When I POST /webhooks/register with `webhook_url: "http://attacker.example/"` (cleartext)<br>Then response MUST be HTTP 400 (scheme rejected)<br>And only `https://` URLs MUST be accepted | status=400 on cleartext scheme | status=200; HTTP URL accepted and persisted | MEDIUM | REPRODUCED | DRIFT-53, evidence: evidence/analysis/04-integration-log.md#drift-53 |
| T-P2-DRIFT-54 | P2-Integration | POST /v1/versioning/upgrade (Versioning) | congruence | versioning | DRIFT-1b — Partner-guide pin endpoint exists only at no-prefix base; not in public docs; returns 200 but is a no-op for our account (previous=current=2026-04-14) | Given partner guide L15 documents `POST /sandbox/v1/versioning/upgrade {"target_version":"2026-04-14"}` as the one-time "pin" call that unlocks the /sandbox stage<br>When I POST the documented URL exactly (`/sandbox/v1/versioning/upgrade`), then POST the no-prefix variant (`/v1/versioning/upgrade`)<br>Then the documented `/sandbox` URL MUST return HTTP 200 with version-state body<br>And the no-prefix variant MUST NOT be the only working path | status=200 at /sandbox/v1/versioning/upgrade | /sandbox variant returns 401 UnauthorizedException; no-prefix variant returns 200 with `{previous_version:"2026-04-14", current_version:"2026-04-14"}` (no-op) | CRITICAL | REPRODUCED | DRIFT-1, DRIFT-1b, DELTA-Bucket-C, evidence: evidence/analysis/04-integration-log.md#drift-1, evidence/work/versioning/01-pin-no-prefix-success.json, evidence/work/versioning/02-pin-sandbox-prefix-fail-401.json, evidence/analysis/13-docs-vs-partner-guide-delta.md |
| T-P2-DRIFT-55 | P2-Integration | POST /sandbox/auth, GET /sandbox/v1/users (Auth+Users) | congruence | sandbox-prod | DRIFT-1c — Two parallel auth/base-URL models (public docs + partner guide) both claim /sandbox prefix; partner-guide pin does NOT unlock the stage; after pin /sandbox/* still returns the same 403/401 envelopes (revalidated 2026-05-28) | Given a successful partner-guide pin via `POST /v1/versioning/upgrade` returning 200<br>When I retry `POST /sandbox/auth` and `GET /sandbox/v1/users?limit=1` with valid Bearer + x-api-key after the pin<br>Then `/sandbox/*` MUST return HTTP 2xx (pin must unlock the stage per partner-guide L8/L15)<br>And the partner-guide flow MUST work end-to-end | status=200 on /sandbox/auth and /sandbox/v1/users after pin | status=403 ForbiddenException on /sandbox/auth (identical to DRIFT-1); status=401 UnauthorizedException on /sandbox/v1/users (identical to DRIFT-2) — pin is a no-op for the stage | CRITICAL | REPRODUCED | DRIFT-1, DRIFT-1c, DRIFT-2, DELTA-Bucket-C, evidence: evidence/analysis/04-integration-log.md#drift-1, evidence/work/versioning/03-sandbox-auth-after-pin-fail-403.json, evidence/work/versioning/04-sandbox-users-after-pin-fail-401.json, evidence/analysis/13-docs-vs-partner-guide-delta.md |
| T-P2-DRIFT-56 | P2-Integration | POST /v1/virtual-accounts/{id}/simulate-deposit (VA) | congruence | sandbox-prod | GAP-22b — Partner-guide documents the sandbox deposit-simulation endpoint at `/sandbox/v1/virtual-accounts/{id}/simulate-deposit` but route is reachable only at the broken `/sandbox` prefix (401) and not at no-prefix base (403 SigV4 leak); endpoint exists but is unusable end-to-end | Given partner guide L90 and prod-cert L49-52 document `POST /v1/virtual-accounts/{id}/simulate-deposit` as the sandbox-only deposit simulator<br>When I POST the no-prefix variant and the `/sandbox` variant with valid Bearer + x-api-key<br>Then exactly one path MUST be reachable with HTTP 2xx<br>And the route MUST be operable end-to-end via a Bearer token from `POST /auth` | exactly one path returns 2xx; route operable | no-prefix path → 403 IncompleteSignatureException (SigV4 leak); /sandbox path → 401 UnauthorizedException; route exists but unreachable for our auth model | HIGH | BLOCKED-ENV | GAP-22a, GAP-22b, DRIFT-1, DRIFT-2, DELTA-Bucket-A, evidence: evidence/analysis/08-flow-design.md#sandbox-deposit-simulation, evidence/work/versioning/05-simulate-deposit-no-prefix-fail-403.json, evidence/work/versioning/06-simulate-deposit-sandbox-prefix-fail-401.json, evidence/analysis/13-docs-vs-partner-guide-delta.md |
| T-P1-META-01 | P1-Docs | cross-cutting (docs surface) | docs-quality | none | META-finding: Public docs portal materially incomplete — real Kira contract distributed via partner-only Word docs + Postman; public-doc-only integrator burns 4-8 days hitting blockers the partner doc resolves on page 1; 22 of 53 drifts acknowledged by partner doc, 0 of 53 acknowledged by public docs | Given a procurement-stage integrator using only the public docs portal (`kira-financial-ai.readme.io`) plus the partner-distributed sandbox-integration-guide.docx and prod-certification-matrix.docx<br>When I diff the partner-doc surface vs the public-doc surface and count acknowledged drifts in each<br>Then the public docs MUST acknowledge ≥80% of the partner-doc-acknowledged drifts<br>And the public docs MUST contain the `POST /v1/versioning/upgrade` pin spec, the Slack workaround for sandbox auto-approve, and the Postman collection link | partner-doc-acknowledged drifts also appear in public docs; canonical contract is public | 22/53 drifts acknowledged in partner doc; 0/53 acknowledged in public docs; pin spec, Slack workaround, Postman collection are partner-only | CRITICAL | EXECUTED | DELTA-meta-finding, Finding-#1, Finding-#2, Finding-#7, Finding-#8, evidence: evidence/analysis/13-docs-vs-partner-guide-delta.md#section-3 |
| T-P2-EP-01 | P2-Integration | POST /auth (Auth) | functional | none | Endpoint probe: response contract honored byte-for-byte | Given valid `client_id`+`password` and the working base URL<br>When I POST /auth<br>Then response MUST be HTTP 200 with `body.data.access_token` + `expires_in: 3600` + `token_type: "Bearer"`<br>And the envelope MUST match docs byte-for-byte (drift confined to URL prefix, DRIFT-1/2) | status=200; envelope matches docs | status=200; envelope matches docs (control passed; URL drift tracked separately) | N/A | CLOSED-FINDING | DRIFT-1, DRIFT-2, evidence: evidence/analysis/04-integration-log.md |
| T-P2-EP-02 | P2-Integration | POST /v1/users (Users, create) | functional | none | Endpoint probe: create-business returns 201 first attempt | Given a fully-formed business user body (with discovered required fields)<br>When I POST /v1/users on the first iteration<br>Then response MUST be HTTP 201<br>And `body.id` MUST be defined | status=201 on iter-1; body.id defined | status=201 on iter-1; body.id defined (drifts tracked in DRIFT-3/4/5/22/24) | N/A | CLOSED-FINDING | DRIFT-3, DRIFT-4, DRIFT-5, DRIFT-22, DRIFT-24, evidence: evidence/analysis/04-integration-log.md |
| T-P2-EP-03 | P2-Integration | GET /v1/countries (Reference Data) | functional | none | Endpoint probe: 250 entries, alpha-3 codes, subdivisions | Given valid Bearer + x-api-key<br>When I GET /v1/countries<br>Then response MUST be HTTP 200 with `body.data.length >= 240`<br>And entries MUST use alpha-3 codes with subdivisions | status=200; ≥240 entries; alpha-3 + subdivisions | status=200; 250 entries; alpha-3 + subdivisions (envelope drift in DRIFT-6 only) | N/A | CLOSED-CLEAN | DRIFT-6 (envelope only), DRIFT-11, evidence: evidence/analysis/04-integration-log.md |
| T-P2-EP-04 | P2-Integration | GET /v1/banks (Reference Data) | functional | iso3166 | Endpoint probe: 2 iter to 2xx; Colombia-only at runtime | Given the documented multi-country expectation<br>When I GET /v1/banks?country_code=CO and ?country_code=MX<br>Then both MUST return HTTP 200 with banks<br>And the endpoint MUST support every documented country | status=200 for any documented country | CO works (200); MX returns 400 `"Only CO supported"` (Colombia-only) | N/A | CLOSED-FINDING | DRIFT-7, DRIFT-8, DRIFT-9, DRIFT-12, evidence: evidence/analysis/04-integration-log.md |
| T-P2-EP-05 | P2-Integration | GET /v1/users (list) | functional | pagination | Endpoint probe: list returns `{data, pagination}` envelope | Given valid auth and at least one created user<br>When I GET /v1/users<br>Then response MUST be HTTP 200 with `body.data[]` and `body.pagination`<br>And country fields in data MUST be normalized (no US/USA mix) | status=200; uniform envelope and country codes | status=200 with envelope correct, but US/USA mix present and limit=100000 → 500 | N/A | CLOSED-FINDING | DRIFT-6, DRIFT-10, DRIFT-11, evidence: evidence/analysis/04-integration-log.md |
| T-P2-EP-06 | P2-Integration | GET /v1/virtual-accounts (list) | functional | pagination | Endpoint probe: empty `data[]` (no VAs); same envelope as users-list | Given a sandbox tenant with no VAs<br>When I GET /v1/virtual-accounts<br>Then response MUST be HTTP 200 with `body.data: []` and `body.pagination` matching users-list shape<br>And ?limit=100000 MUST NOT 500 | status=200 with same envelope; limit=100000 capped/400 | status=200 with empty data[]; limit=100000 → HTTP 500 (DRIFT-10) | N/A | CLOSED-FINDING | DRIFT-6, DRIFT-10, evidence: evidence/analysis/04-integration-log.md |
| T-P2-EP-07 | P2-Integration | GET /v1/recipients (list) | functional | envelope | Endpoint probe: 400 by design (needs user_id); list shape captured | Given /v1/recipients requires `user_id`<br>When I GET /v1/recipients without `user_id`, then with `user_id`<br>Then 400 envelope MUST match Shape A<br>And list envelope MUST be `{data, pagination}` | Shape A 400; `{data,pagination}` list | Shape B 400 (nested) and `{recipients,total}` list — both drifts | N/A | CLOSED-FINDING | DRIFT-6, DRIFT-13, DRIFT-33, evidence: evidence/analysis/04-integration-log.md |
| T-P2-EP-08 | P2-Integration | GET /v1/users/{id} (Users) | functional | none | Endpoint probe: 1 iter to 2xx; 10-sample latency median 339ms | Given a created user id<br>When I GET /v1/users/{id} and capture latency over 10 samples<br>Then response MUST be HTTP 200<br>And `body.verification_triggered` MUST be present (POST/GET parity) | status=200 with verification_triggered present | status=200; verification_triggered missing on GET (DRIFT-14); median 339ms | N/A | CLOSED-FINDING | DRIFT-14, evidence: evidence/analysis/04-integration-log.md |
| T-P2-EP-09 | P2-Integration | PUT /v1/users/{id} (Users) | functional | none | Endpoint probe: 3 iter to 2xx; empty body returns 200 (drift) | Given a created user id and a valid update body<br>When I PUT /v1/users/{id} (iter to 2xx)<br>Then response MUST be HTTP 200 with `body.updated_fields[] != []`<br>And empty body MUST return 400 (DRIFT-15) | status=200; updated_fields non-empty; empty body 400 | status=200 reached in 3 iter; DRIFTs 15/19/20 confirmed on validation | N/A | CLOSED-FINDING | DRIFT-15, DRIFT-19, DRIFT-20, evidence: evidence/analysis/04-integration-log.md |
| T-P2-EP-10 | P2-Integration | POST /v1/users/{id}/verifications | functional | idempotency | Endpoint probe: 1 iter to 2xx; AiPrise URL minted but no state move | Given a user id and a verification body<br>When I POST /v1/users/{id}/verifications<br>Then response MUST be HTTP 201<br>And user state MUST advance AND idempotency MUST be enforced | 201 + state advance + 409 on key reuse | 201 reached iter-1; state unchanged (DRIFT-17); idempotency not enforced (DRIFT-16) | N/A | CLOSED-FINDING | DRIFT-16, DRIFT-17, DRIFT-23, DRIFT-25, evidence: evidence/analysis/04-integration-log.md |
| T-P2-EP-11 | P2-Integration | POST /verification/send | functional | path-prefix | Endpoint probe: OTP path found at root after 2 iter | Given OTP is expected under `/v1/`<br>When I POST /v1/verification/send and then root /verification/send<br>Then /v1/ path MUST be HTTP 200<br>And the request MUST be keyed by `user_id` | /v1/ works; user-keyed | /v1/ 404; works only at root, keyed by email (DRIFT-21) | N/A | CLOSED-FINDING | DRIFT-21, GAP-23, evidence: evidence/analysis/04-integration-log.md |
| T-P2-EP-12 | P2-Integration | POST /v1/recipients (SPEI MX) | functional | none | Endpoint probe: 1 iter to 2xx; SPEI body schema canonical | Given a valid SPEI MX recipient body with CLABE<br>When I POST /v1/recipients on first iteration<br>Then response MUST be HTTP 201<br>And country/account_details MUST round-trip without silent rewrite | status=201; round-trip preserved | status=201 iter-1; subsequent DRIFTs on country override (38) + plaintext details (30) | N/A | CLOSED-FINDING | DRIFT-31, DRIFT-32, DRIFT-38, evidence: evidence/analysis/04-integration-log.md |
| T-P2-EP-13 | P2-Integration | POST /v1/recipients (ACH USD) | functional | none | Endpoint probe: 2 iter to 2xx; doc_type "ssn" rejected | Given ACH USD recipient body per docs (includes `doc_type:"ssn"`)<br>When I POST /v1/recipients<br>Then response MUST be HTTP 201 on iter-1<br>And `doc_type:"ssn"` MUST be accepted | 201 on iter-1 with doc_type=ssn | 201 reached on iter-2 (ssn rejected, replaced with `id`); DRIFTs 27/28 | N/A | CLOSED-FINDING | DRIFT-27, DRIFT-28, evidence: evidence/analysis/04-integration-log.md |
| T-P2-EP-14 | P2-Integration | POST /v1/recipients (USDT TRON) | functional | none | Endpoint probe: 2 iter to 2xx; base58check validation discovered | Given a USDT TRON recipient body with TRON-length address<br>When I POST /v1/recipients<br>Then response MUST be HTTP 201 on iter-1 (length-only per docs)<br>And cryptographic checks MUST NOT exist beyond length | 201 on iter-1 (length-only) | 201 reached on iter-2 (base58check enforced, undocumented; DRIFT-29) | N/A | CLOSED-FINDING | DRIFT-29, evidence: evidence/analysis/04-integration-log.md |
| T-P2-EP-15 | P2-Integration | POST /v1/recipients (SWIFT EUR) | functional | none | Endpoint probe: 2 iter to 2xx; address required + POST/GET divergence | Given a SWIFT EUR recipient body<br>When I POST /v1/recipients and then GET /v1/recipients/{id}<br>Then response MUST be HTTP 201 on iter-1<br>And state/postal_code MUST be returned identically on POST and GET | 201 iter-1; POST==GET on address fields | 201 on iter-2 (address required); state/postal_code="" on POST (DRIFT-32) | N/A | CLOSED-FINDING | DRIFT-27, DRIFT-30, DRIFT-33, evidence: evidence/analysis/04-integration-log.md |
| T-P2-EP-16 | P2-Integration | GET /v1/recipients/{id} (4 variants) | functional | none | Endpoint probe: 1 iter to 2xx for all 4; unmasked details | Given recipients created for SPEI, ACH, USDT, SWIFT variants<br>When I GET /v1/recipients/{id} for each<br>Then all 4 MUST be HTTP 200<br>And account_details MUST be masked (last-4 only) | 200 × 4 with masked details | 200 × 4 with plaintext details (DRIFT-30 broadens across all variants) | N/A | CLOSED-FINDING | DRIFT-31, DRIFT-33, DRIFT-30, evidence: evidence/analysis/04-integration-log.md |
| T-P2-EP-17 | P2-Integration | POST /v1/quotations (Quotations) | functional | none | Endpoint probe: 0/22 happy paths reachable (blocked by fee profiles) | Given a documented quotation request body<br>When I POST /v1/quotations across 22 amount/currency permutations<br>Then at least 1/22 MUST return HTTP 200 with `quote_id`<br>And the sandbox MUST permit a happy path | ≥1/22 returns 200 with quote_id | 0/22 reachable — blocked by fee profiles ≥100% (DRIFT-45) | N/A | BLOCKED-ENV | DRIFT-40, DRIFT-41, DRIFT-42, DRIFT-43, DRIFT-44, DRIFT-45, DRIFT-46, evidence: evidence/analysis/04-integration-log.md |
| T-P2-EP-18 | P2-Integration | POST /webhooks/register (Webhooks) | functional | webhook-semantics | Endpoint probe: 1 iter to 2xx (docs body); SSRF + opaque surface | Given a docs-shape webhook body with HTTPS URL + secret<br>When I POST /webhooks/register<br>Then response MUST be HTTP 200 with a webhook `id` AND HMAC contract<br>And SSRF/scheme/idempotency drifts MUST be absent | 200 with id + secure semantics | 200 iter-1 but body has no id; DRIFT-47..53 expose SSRF + opaque + auth/idem drift | N/A | CLOSED-FINDING | DRIFT-47..53, evidence: evidence/analysis/04-integration-log.md |
| T-P3-SEC-F1 | P3-Security | POST /webhooks/register → outbound (Webhooks) | security | webhook-semantics | SSRF: Kira fetches attacker-supplied webhook URL at delivery (IMDS reachable) | Given POST /webhooks/register accepts arbitrary URLs (DRIFT-47)<br>When I register webhook_url=http://169.254.169.254/latest/meta-data/ and trigger a delivery event<br>Then Kira's egress MUST be blocked (no outbound to IMDS)<br>And the URL MUST be rejected at registration | no outbound to attacker URL; reg rejected | outbound delivery hits attacker URL; IMDS reachable | CRITICAL | REPRODUCED | DRIFT-47, OWASP-API7:2023, GAP-11, GAP-21, evidence: evidence/analysis/05-security-audit.md#finding-1 |
| T-P3-SEC-F2 | P3-Security | GET /v1/users (list+detail) | security | info-disclosure | PII unmasked — SSN/document_number plaintext on list and detail | Given a tenant with users that have SSN/document_number<br>When I GET /v1/users and GET /v1/users/{id}<br>Then `body.data[].document_number` MUST be masked to last-4<br>And no record MUST return plaintext SSN | last-4 mask `****1234` on every record | full plaintext SSN/document_number on list and detail | CRITICAL | REPRODUCED | DRIFT-30 (broadened), OWASP-API3:2023, evidence: evidence/analysis/05-security-audit.md#finding-2 |
| T-P3-SEC-F3 | P3-Security | GET /v1/recipients/{id} (4 variants) | security | info-disclosure | PII unmasked — account_details plaintext on all 4 recipient variants | Given 4 recipients (SPEI/ACH/USDT/SWIFT)<br>When I GET /v1/recipients/{id} for each<br>Then account_details MUST be masked (last-4)<br>And clabe/routing/iban/wallet MUST NOT be plaintext | masked last-4 on every variant | plaintext clabe/routing/iban/wallet on all 4 variants | CRITICAL | REPRODUCED | DRIFT-30, OWASP-API3:2023, evidence: evidence/analysis/05-security-audit.md#finding-3 |
| T-P3-SEC-F4 | P3-Security | cross-cutting (TLS) | security | none | TLS 1.0 and TLS 1.1 accepted at api.balampay.com:443 | Given api.balampay.com:443<br>When I `openssl s_client -tls1` and `-tls1_1` against the host<br>Then both handshakes MUST fail (TLS 1.2 minimum)<br>And only TLS 1.2+ ciphers MUST be negotiated | handshake fails for TLSv1 and TLSv1.1 | handshake succeeds for TLSv1 and TLSv1.1 (legacy ciphers accepted) | CRITICAL | REPRODUCED | OWASP-API8:2023, evidence: evidence/analysis/05-security-audit.md#finding-4 |
| T-P3-SEC-F5 | P3-Security | POST /v1/users (Users) | security | mass-assignment | Mass assignment: `verification_mode` is integrator-settable | Given `verification_mode` is server-derived per design<br>When I POST /v1/users with `verification_mode: "verification_link"`<br>Then response MUST ignore the field OR HTTP 400<br>And subsequent GET MUST NOT echo the integrator-set value | field stripped or 400; GET ignores | field accepted and echoed; `verification_link` flow actually works | HIGH | REPRODUCED | DRIFT-4, OWASP-API3:2023, evidence: evidence/analysis/05-security-audit.md#finding-5 |
| T-P3-SEC-F6 | P3-Security | GET /v1/users/{id} 404 | security | envelope | Error envelope drift: 5th distinct shape on 404 vs /v1/recipients 404 | Given /v1/recipients 404 returns `{error:{code,message}}`<br>When I GET /v1/users/{unknown_id}<br>Then response envelope MUST match the recipients 404 shape<br>And no new envelope shape MUST appear on 404 | both 404s share `{error:{code,message}}` | /v1/users 404 returns `{code, message}` (5th distinct shape) | LOW | REPRODUCED | DRIFT-6, OWASP-API8:2023, evidence: evidence/analysis/05-security-audit.md#finding-6 |
| T-P3-SEC-F7 | P3-Security | OPTIONS /v1/users (CORS) | security | none | CORS `Access-Control-Allow-Origin: *` on preflight (inconsistent with GET) | Given GET /v1/users returns a specific allowed origin<br>When I OPTIONS /v1/users with an Origin header<br>Then `Access-Control-Allow-Origin` MUST match the specific allowed origin<br>And wildcard `*` MUST NOT be returned on preflight | header echoes specific origin | header = `*` on OPTIONS (inconsistent with GET) | LOW | REPRODUCED | OWASP-API8:2023, evidence: evidence/analysis/05-security-audit.md#finding-7 |
| T-P3-SEC-F8 | P3-Security | cross-cutting (headers) | security | none | Missing `Permissions-Policy` and `Cache-Control` headers | Given OWASP-API8 baseline hardening headers<br>When I capture any response from api.balampay.com<br>Then `Permissions-Policy` MUST be present<br>And `Cache-Control` MUST be present | both headers present | both headers absent | LOW | REPRODUCED | OWASP-API8:2023, evidence: evidence/analysis/05-security-audit.md#finding-8 |
| T-P3-SEC-F9 | P3-Security | GET unknown-path (gateway) | security | info-disclosure | SigV4 base64 hash leak on unknown-path 403 | Given any unknown path on api.balampay.com<br>When I GET /unknown-path<br>Then response MUST be HTTP 403 with a generic forbidden body<br>And no SHA-256/base64/SigV4 hash MUST appear in the body | generic forbidden body | 403 with SigV4 SHA-256/base64 hash in body | LOW | REPRODUCED | DRIFT-39, OWASP-API8:2023, evidence: evidence/analysis/05-security-audit.md#finding-9 |
| T-P3-SEC-C1 | P3-Security | cross-cutting (JWT) | security | auth | Control: JWT attack suite — 9 forgeries all rejected (alg=none, kid, HS256) | Given the JWT attack suite (9 vectors: alg=none, alg=HS256-with-RS256-key, kid injection, etc.)<br>When I send each forged token<br>Then every request MUST be HTTP 401 or 403<br>And no forgery MUST succeed (control passed) | 9/9 attacks rejected (401/403) | 9/9 attacks rejected — control matches expected | N/A | CLOSED-CLEAN | OWASP-API2:2023, evidence: evidence/work/security/jwt-attack-suite/ |
| T-P3-SEC-C2 | P3-Security | GET /v1/users\|recipients\|virtual-accounts/{id} | security | none | Control: BOLA cross-tenant via UUID enumeration — 16 attempts, 0 leaks | Given 16 random-UUID GET probes across users/recipients/virtual-accounts<br>When I issue each GET with a foreign-tenant id guess<br>Then every response MUST be HTTP 404<br>And no resource MUST leak (control passed) | 16/16 return 404 | 16/16 return 404 — control matches expected | N/A | CLOSED-CLEAN | OWASP-API1:2023, evidence: evidence/work/security/bola-id-enumeration/ |
| T-P3-ABUSE-1 | P3-Abuse | POST/DELETE /v1/recipients (Recipients) | abuse | none | delete-recipient-pollution: 20/20 DELETE 403 SigV4 leak; pagination ignored | Given 20 created recipients with pagination params<br>When I DELETE /v1/recipients/{id} on each AND GET /v1/recipients?limit=5<br>Then DELETE MUST return 200/204/405 (no SigV4 leak)<br>And pagination params (limit/offset) MUST be honored | DELETE 200/204/405; pagination honored | 20/20 DELETE 403 SigV4 leak; pagination silently ignored | HIGH | REPRODUCED | DRIFT-39, ABUSE-1, evidence: evidence/analysis/06-abuse-scenarios.md#scenario-1 |
| T-P3-ABUSE-4 | P3-Abuse | POST /webhooks/register (Webhooks) | abuse | webhook-semantics | webhook-spoof: 3/3 bogus `client_uuid` accepted — cross-tenant hijack vector | Given my own client_uuid is the tenant boundary<br>When I POST /webhooks/register with 3 random foreign `client_uuid` values<br>Then all 3 MUST return HTTP 403 (tenant mismatch)<br>And foreign tenant webhooks MUST NOT be registerable | 3/3 = 403 | 3/3 = 200 (foreign tenant webhooks registered) | CRITICAL | REPRODUCED | DRIFT-51, ABUSE-4, OWASP-API3:2023, evidence: evidence/analysis/06-abuse-scenarios.md#scenario-5 |
| T-P3-ABUSE-2 | P3-Abuse | POST /v1/recipients (ACH USD) | abuse | iso3166 | silent-country-override: ACH US-routing accepts address.country=MX (AML/OFAC risk) | Given an ACH USD recipient with a valid US routing number<br>When I POST /v1/recipients with `address.country: "MX"` (routing ≠ country)<br>Then response MUST be HTTP 400 (cross-check routing vs country)<br>And routing/country mismatch MUST be rejected | status=400 routing-vs-country mismatch | status=201; mismatch silently accepted (AML/OFAC risk) | HIGH | REPRODUCED | DRIFT-38, ABUSE-2, evidence: evidence/analysis/06-abuse-scenarios.md#scenario-4 |
| T-P3-ABUSE-3 | P3-Abuse | POST /v1/recipients (WALLET TRON) | abuse | iso3166 | silent-country-strip: WALLET silently strips country (vs SPEI override vs ACH accept) | Given the same endpoint behaves consistently across recipient types<br>When I POST /v1/recipients for SPEI/ACH/WALLET each with country="USA"<br>Then country handling MUST be uniform across all 3 types<br>And no type MUST silently strip/override/accept differently | uniform country handling | 3 distinct behaviors: SPEI override, ACH accept, WALLET strip | HIGH | REPRODUCED | DRIFT-38, ABUSE-3, evidence: evidence/analysis/06-abuse-scenarios.md#scenario-4 |
| T-P3-ABUSE-5 | P3-Abuse | POST /v1/virtual-accounts (Virtual Accounts) | abuse | envelope | verification-skip: 4th error envelope `{statusCode, error, message, timestamp}` discovered | Given the API should emit a single error envelope<br>When I POST /v1/virtual-accounts and trigger a verification-state error<br>Then envelope MUST match the canonical Shape A<br>And no Nest-style `{statusCode,error,message,timestamp}` MUST appear | Shape A envelope | Shape D (Nest-style) — 4th distinct envelope captured | HIGH | REPRODUCED | DRIFT-6, ABUSE-5, GAP-05, evidence: evidence/analysis/06-abuse-scenarios.md#scenario-6 |
| T-P3-ABUSE-6 | P3-Abuse | POST /v1/virtual-accounts (Virtual Accounts) | abuse | none | verification-skip: validation-layer ordering depends on `type` discriminator | Given a malformed VA body that triggers BOTH schema and verification errors<br>When I POST /v1/virtual-accounts with type=US_BANK then with type=MX_SPEI<br>Then both MUST return the SAME validation layer first (schema or verification)<br>And error ordering MUST NOT branch on `type` | uniform layer order across types | US_BANK fails schema-first; MX_SPEI fails verification-first | MEDIUM | REPRODUCED | ABUSE-6, evidence: evidence/analysis/06-abuse-scenarios.md#scenario-6 |
| T-P3-ABUSE-7 | P3-Abuse | POST /v1/virtual-accounts (Virtual Accounts) | abuse | info-disclosure | verification-skip: error echoes user `status` — enumerable verification-state oracle | Given a user with status=REVIEW<br>When I POST /v1/virtual-accounts as that user<br>Then error message MUST be opaque (e.g., "verification required")<br>And response MUST NOT echo `"Current status: REVIEW"` (state oracle) | opaque error string | error contains `"Current status: REVIEW"` — verification-state oracle | MEDIUM | REPRODUCED | ABUSE-7, evidence: evidence/analysis/06-abuse-scenarios.md#scenario-6 |
| T-P3-ABUSE-8 | P3-Abuse | POST /v1/recipients (Recipients) | abuse | idempotency | Control: idempotency RACE-SAFE — 10 parallel + variants behave per Stripe pattern | Given 10 parallel POST /v1/recipients with same Idempotency-Key + identical body<br>When all 10 fire concurrently<br>Then exactly 1 MUST return 201 with a unique id<br>And the other 9 MUST return 409 with the same id (Stripe-pattern) | 1×201 + 9×409 referencing same id | 1×201 + 9×409 referencing same id — control matches expected | N/A | CLOSED-CLEAN | DRIFT-50 (contrast), ABUSE-8, evidence: evidence/analysis/06-abuse-scenarios.md#scenario-2 |
| T-P3-ABUSE-C1 | P3-Abuse | GET /v1/users\|recipients\|VA/{id} | abuse | none | Control: BOLA via random UUID stub — 23/23 returned 404 (no leak) | Given 23 random-UUID GET probes across users, recipients, VAs<br>When I issue each GET<br>Then every response MUST be HTTP 404<br>And no resource MUST leak (control passed) | 23/23 = 404 | 23/23 = 404 — control matches expected (schema-gated POSTs not in scope) | N/A | CLOSED-CLEAN | evidence: evidence/analysis/06-abuse-scenarios.md#scenario-3 |
| T-P3-ABUSE-C2 | P3-Abuse | POST /v1/virtual-accounts (Virtual Accounts) | abuse | state-casing | Control: verification check WORKS on MX_SPEI path (no state-machine bypass) | Given a user with status=REVIEW (not yet VERIFIED)<br>When I POST /v1/virtual-accounts of type=MX_SPEI<br>Then response MUST be HTTP 400 with explicit verification-required message<br>And no VA MUST be created (control passed) | status=400 verification required; no VA | status=400 verification required; no VA — control matches expected | N/A | CLOSED-CLEAN | DRIFT-23, evidence: evidence/analysis/06-abuse-scenarios.md#scenario-6 |
| T-P3-LOAD-1 | P3-Performance | POST /auth (Auth) | performance | none | auth-cold-warm-latency: cold/warm ratio 1.04× over 30 calls; p99=1503ms | Given 30 sequential POST /auth calls (cold then warm)<br>When I measure cold-call latency vs warm-call median<br>Then cold/warm ratio MUST be ≥ 1.5× (visible cold-start penalty)<br>And p99 MUST be < 1500ms | cold/warm ratio ≥ 1.5×; p99 < 1500ms | cold/warm ratio = 1.04× (no cold start); p99 = 1503ms with 2 tail outliers | MEDIUM | REPRODUCED | DRIFT-1 (lineage), evidence: evidence/analysis/07-load-summary.md#scenario-1 |
| T-P3-LOAD-2 | P3-Performance | GET /v1/users (list) | performance | pagination | list-pagination-depth-latency: `/v1/users?limit=500` already returns 500 | Given DRIFT-10 shows limit=100000 → 500<br>When I GET /v1/users?limit=500<br>Then response MUST be HTTP 200 (well below the 100000 ceiling)<br>And the safe ceiling MUST be ≥ 500 | limit=500 → 200 | limit=500 → 500 (ceiling much lower than expected) | HIGH | REPRODUCED | DRIFT-10, evidence: evidence/analysis/07-load-summary.md#scenario-2 |
| T-P3-LOAD-3 | P3-Performance | POST /v1/recipients (Recipients) | performance | none | concurrent-create-recipients: no race signal; 2.66× latency degradation at N=20 | Given N=20 concurrent POST /v1/recipients with distinct bodies<br>When I measure median latency under load vs single-call baseline<br>Then degradation factor MUST be ≤ 1.5×<br>And no race-condition signal MUST appear (control passed for race) | ≤1.5× degradation; no race | uniform 400s; 2.66× median latency degradation; no race signal | LOW | REPRODUCED | evidence: evidence/analysis/07-load-summary.md#scenario-3 |
| T-P3-LOAD-4 | P3-Performance | GET /v1/countries (Reference Data) | performance | none | rate-limit-discovery: no 429 up to 20 rps × 15s (544 reqs) — no documented limit | Given the API documents a rate limit somewhere<br>When I GET /v1/countries at 20 rps for 15s (≥500 requests)<br>Then at least one response MUST be HTTP 429<br>And response MUST include `Retry-After` header | ≥1 × 429 with Retry-After | 0 × 429 across 544 requests; no Retry-After header observed | MEDIUM | REPRODUCED | evidence: evidence/analysis/07-load-summary.md#scenario-4 |
| T-P3-LOAD-5 | P3-Performance | POST /v1/quotations (Quotations) | performance | none | quotations-error-latency-under-load: 4xx path 2.43× degradation; no 429/5xx | Given DRIFT-45 forces a 4xx error path<br>When I POST /v1/quotations under sustained load (sequential bursts)<br>Then 4xx median latency degradation MUST be ≤ 1.5×<br>And no 429/5xx MUST appear | ≤1.5× degradation; no 429/5xx | 2.43× median degradation; p99 ~840ms (ceiling-bound); no 429/5xx | LOW | REPRODUCED | DRIFT-45, evidence: evidence/analysis/07-load-summary.md#scenario-5 |
